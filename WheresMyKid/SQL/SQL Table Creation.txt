CREATE TABLE users 
(
user_ID VARCHAR(32) PRIMARY KEY,
password VARCHAR(255) NOT NULL,
tags VARCHAR(255)
);

CREATE TABLE tags 
(
tag_ID INT AUTO_INCREMENT,
tag_address VARCHAR(255),
PRIMARY KEY(tag_ID)
);


ALTER TABLE users
ADD FOREIGN KEY (tags) REFERENCES tags(tag_ID);

